let name = 'Mosh';
console.log(name);

// var name cannot be a reserved keyword
// should be meaningful
// cannot start with a number
// cannot contain space or a hyphen

let firstName = 'harasees';
let lastName = 'singh';

const interestRate = 0.3;
// interestRate = 1; // error
console.log(interestRate);

// 2 types of variables
// Primitives and Reference Types

// Primitive Types:
// String
// boolean
// Number
// undefined
// null

let age = 30; // number literal
let isApproved = true; // boolean literal
let firstName__ = null; // to explicitly clear the value of a variable


// Javascript is a dynamically typed language
// In static languages when we declare a variable it's type cannot be changed;
// In dynamically typed languages it can change at runtime;

let n = 'something';
console.log(typeof n);

n = 9.00;
console.log(typeof n);

console.log(`i do a lot of math like ${3 + 4}`);

// in js we don't have integers and floating point numbers; they are all numbers

// Reference Types:
// Object
// Array
// Function

