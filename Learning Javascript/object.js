
let person = {
    name:'harasees', age:18
};

console.log(person);

let selection = 'name';
person[selection] = 'mary';

console.log(person.name);

// initializing an array;
// an array is an object actually;

let selectedColors = ['red', 'blue', 4];

selectedColors[4] = 'grey';

console.log(selectedColors);

console.log(Math.PI + Math.E + Math.random());

console.log(Math.floor(Math.random()*10));



